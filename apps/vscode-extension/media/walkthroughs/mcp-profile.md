# Pick a focused profile

Profiles reduce the server surface to the tools that match the current workflow. Use `pcb_only`, `schematic_only`, `manufacturing`, or `analysis` when a full tool surface is unnecessary.

# Configure the workspace

Generate `.vscode/mcp.json` for the open KiCad workspace. KiCad Studio remains fully usable without this file; it only enables MCP-backed gates, fixes, and release helpers.
